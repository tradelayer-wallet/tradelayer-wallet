import { Injectable } from "@angular/core";
import ltcUtils from '../../utils/litecore.util'

@Injectable({
    providedIn: 'root',
})

export class AuthService {
    constructor() {}

    register() {
        // const pair = ltcUtils.generateRandomAddress();
        // console.log({pair});
    }
}
